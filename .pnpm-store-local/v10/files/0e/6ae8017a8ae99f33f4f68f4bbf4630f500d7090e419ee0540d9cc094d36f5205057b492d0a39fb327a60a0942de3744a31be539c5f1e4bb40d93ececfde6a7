/* Escape-hatch proof: extra stylesheet appended after the theme via config.css. */
:root {
	--example-extra-css-loaded: 1;
}
.example-custom-edit {
	display: inline-block;
	font-size: 0.875rem;
	color: var(--se-pine);
}
