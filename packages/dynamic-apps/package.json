{
	"name": "@rivet-dev/dynamic-apps",
	"version": "0.12.0-rc.2",
	"description": "Build and run user-generated HTTP applications in agentOS with durable Rivet state.",
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/rivet-dev/dynamic-apps.git",
		"directory": "packages/dynamic-apps"
	},
	"type": "module",
	"sideEffects": false,
	"files": [
		"dist",
		"package.json"
	],
	"exports": {
		".": {
			"import": {
				"types": "./dist/index.d.ts",
				"default": "./dist/index.js"
			}
		}
	},
	"engines": {
		"node": ">=22.0.0"
	},
	"scripts": {
		"build": "tsup src/index.ts --format esm --dts --sourcemap --clean --external rivetkit --external @rivet-dev/agentos-core --external @rivet-dev/agentos-toolchain --external @rivet-dev/dynamic-apps-builder --external @agentos-software/sh --external @agentos-software/tar",
		"check-types": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@rivet-dev/dynamic-apps-core": "workspace:0.12.0-rc.2",
		"hono": "^4.7.0",
		"rivetkit": "2.3.11"
	},
	"devDependencies": {
		"@rivet-dev/agentos-core": "0.2.15",
		"@rivet-dev/agentos-toolchain": "0.2.15",
		"@types/node": "^22.19.15",
		"tsup": "^8.4.0",
		"typescript": "^5.7.3",
		"vitest": "^2.1.8"
	}
}
