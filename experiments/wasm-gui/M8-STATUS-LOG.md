2026-06-24T08 (loop iter1): M8.6 root cause CORRECTED — blocker is X-server (Xvfb) internal futex storm under openbox+ANY-client (main-dispatch wasm busy-loop ⇄ input-thread net_poll, V8 FutexEmulation). NOT pcmanfm/HarfBuzz/paint. Ruled out: net_poll throttle, poll-waiter pool, -dumbSched input thread. Fix target: crates/v8-runtime wasm-threads / setitimer shim. Proof: gui-progress/2026-06-24T08/proof-m8.6-openbox-xclock-BLACK.png (black).
2026-06-24T08 (loop iter3): BROKE symbolization wall (SECURE_EXEC_KEEP_NAMES Xvfb keeps name section = M8.1 symbolizer DONE). SYMBOLIZED the M8.6 spin: Xvfb dispatch loop WaitForSomething→vfbBlockHandler→pwrite(1.2MB fb) every iteration because WaitForSomething doesn't block; the 1.2MB sync-RPC writeSync per cycle IS the futex storm. Next: host epoll bridge — which client fd is stuck-ready (openbox post-BadAlloc?). Proof: gui-progress/2026-06-24T08/proof-m8.6-named-xvfb-symbolized-BLACK.png
2026-06-24T09 (loop iter5): traced WaitForSomething timeout=0 ← clients_are_ready() perpetually true (client stuck on ready/output_pending list, dix/dispatch.c:275 + os/io.c:666). X server polls t=0 ready=0 ~43/s; each iter writes 1.2MB fb = the CPU. openbox does POLLOUT-only spin ~13/s (net_poll unconditional POLLOUT). Writes non-partial so output_pending isn't short-writes. Next: named openbox stackdump + which ready-list path. Proof: gui-progress/2026-06-24T09/proof-m8.6-waitforsomething-timeout0-BLACK.png
2026-06-24T09 (loop iter5): named openbox confirms the net_poll spinner is the X SERVER WaitForSomething (openbox parked). Read io.c: WriteToClient buffers+output_pending_mark unless (count==0 && who->local); FlushClient registers POLLOUT only on EAGAIN (our writes never short). Perpetual ready needs who->local FALSE, or continuous re-generated output, or read-path buffered request. Next: TEMP ErrorF in io.c mark_client_ready (constraint-#5 allows reverted instrumentation) to pinpoint client+path, rebuild Xvfb, read, revert.
2026-06-24T09 (loop iter6-7): DEFINITIVE via X-server temp-instrumentation — openbox (c1) is in a TRUE infinite re-setup loop (GrabKey-heavy ~40req/s, 90s still black), keeping ready-list non-empty → WaitForSomething timeout=0 → 1.2MB fb-write/cycle = the spin. Ruled out XKB (staged, 0 errors, still spins) + MappingNotify. Lead: openbox re-managing screen on repeated RandR/screen-change events (cf M8.4 Xvfb RandR phantom). Next: instrument X-server event-send / try -extension RANDR. Proof: gui-progress/2026-06-24T09/proof-m8.6-openbox-xclock-90s.png
2026-06-24T09 (loop iter8): openbox re-setup loop is NOT event-driven (X server sends <100 events/20s via WriteEventsToClient probe) and NOT RandR (-extension RANDR still spins). So openbox spins INTERNALLY re-applying keybindings. Unifying hypothesis: guest monotonic-clock/timer or select-timeout math is off → both openbox's main loop AND X-server WaitForSomething compute timeout=0 (timers always due) → spin. Next: named openbox stackdump + inspect clock_gettime/select-timeout path. Proof: gui-progress/2026-06-24T09/proof-m8.6-no-randr.png
2026-06-24T10 (loop iter9): ★ M8.6 RENDERS at 160x120 (98% nonblack, CPU 210→44j settled). ROOT CAUSE = fb-write throughput feedback loop: vfbBlockHandler pwrites whole 1.2MB fb every block, base64-encoded per sync-RPC (encodeSyncRpcValue:10981), starving the X server → openbox setup never finishes → WaitForSomething timeout=0 → spin. Fix (runtime): large binary sync-RPC via 4MB dataBuffer SAB not base64. Proofs: gui-progress/2026-06-24T10/proof-m8.6-tiny-screen-160x120.png (RENDERS), proof-m8.6-openbox-xclock-90s.png (black at 640x480).
